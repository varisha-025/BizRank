import React from 'react'

const BusinessPage = ({ brandName, brandDesc, brandLogo, productCategories, paymentOptions, deliveryTime, socialMediaHandles, brandContactEmail, brandWebsite }) => {
  return (
    <div>Business Page for {brandName}</div>
    
  )
}

export default BusinessPage